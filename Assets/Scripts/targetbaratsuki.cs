using UnityEngine;
using System.Collections;


 public class targetbaratsuki : MonoBehaviour
    {
       /* private float InstantiationTimer = 10f;
        public Transform target;//☆
        private void follow()
        {
        InstantiationTimer -= Time.deltaTime;
        Vector3 offset = new Vector3(Random.Range(-2f,2f),Random.Range(-2f,2.0f), 6f);
        transform.position = target.position + offset;
        InstantiationTimer =5f;

        }

        private void Update()
        {
            follow();
        }*/
    }


